Recording Studio Web Reader install complete.

Next steps:

1. Review config/initializers/recording_studio_web_reader.rb and set any required options.
2. If you use environment-specific settings, create config/recording_studio_web_reader.yml.
3. Copy engine migrations with `bin/rails generate recording_studio_web_reader:migrations` when the gem ships migrations.
